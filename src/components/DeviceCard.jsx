export default function DeviceCard({ title }) {
  return (
    <div className="device-card">
      <div className="device-header">{title}</div>
      <div className="device-body">
        {/* Data monitoring */}
      </div>
    </div>
  );
}